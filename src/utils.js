// Utilitas untuk memformat pesan yang diterima
function formatMessage(text) {
    return text.trim().toLowerCase();
}

// Fungsi tambahan lainnya seperti validasi, pengolahan input bisa ditambahkan di sini

module.exports = { formatMessage };
